[Unit]
Description=Asterisk Reloader -Component of The Open Source Contact Center Solution
Documentation= http://www.omnileads.net
After=syslog.target network-online.target

[Service]
Type=simple
User=omnileads
EnvironmentFile=/etc/omnileads-asterisk.env
ExecStartPre=/bin/bash -c 'source /etc/asterisk/virtualenv/bin/activate'
ExecStart=/opt/omnileads/asterisk/virtualenv/bin/python3 /etc/asterisk/virtualenv/regenerar_asterisk.py
ExecStop=/usr/bin/pkill -f "python3 regenerar_asterisk.py"
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
